# Python FastAPI

Use when building or changing Python FastAPI services.

Prefer small, typed FastAPI routers, Pydantic request and response models, dependency-injected resources, explicit error responses, and focused tests with `pytest` or `httpx` test clients. Keep blocking I/O out of async handlers unless it is intentionally isolated.
