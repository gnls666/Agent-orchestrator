# React

Use when building or changing React user interfaces.

Prefer simple component boundaries, stable props, accessible controls, derived render state instead of effect-driven copies, and CSS/layout choices that hold across desktop and mobile. Verify visible behavior with browser checks for user-facing UI changes.
