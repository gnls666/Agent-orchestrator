# TypeScript

Use when building or changing TypeScript code.

Prefer precise domain types, narrow public interfaces, discriminated unions for state, runtime validation at trust boundaries, and tests that cover behavior instead of implementation details. Avoid `any` unless a boundary genuinely cannot be typed more safely.
